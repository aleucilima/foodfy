{% extends "layout.njk" %}

{% block head %}
    <link rel="stylesheet" type="text/css" href="/styles.css" media="screen" />
{% endblock %}

{% block content %}
    <section class="wrapper-recipe">
        <div class="recipe">
            <div class="recipe_img">
                <img src="{{items.image}}" alt="Imagem de {{items.title}}">
            </div>
            <div class="content-recipe">
                <p class="revenue">{{items.title}}</p>
                <p class="author">{{items.author}}</p>
            </div>
        </div>

    <h2 id="ingredients">Ingredientes</h2>

    {% for ingredients in items.ingredients %}
        <li>
            {{ingredients}}
        </li>
    {% endfor %}

    <h2 id="preparation">Modo de Preparo</h2>

    <h2 id="information">Informações adicionais</h2>
    <p id="information-recipe">{{items.information}}</p>

    </section>
{% endblock %}
