{% extends "layout.njk" %}

{% block content %}
    <div class="card">
        <div class="card_img">
            <img src="{{recipe.image}}" alt="burger">
        </div>
        <div class="content">
            <p class="revenue">{{recipe.title}}</p>
            <p class="author">{{recipe.author}}</p>
        </div>
    </div>
{% endblock %}
