{% extends "layout.njk" %}

{% block content %}
        <section>
        {% for item in items %}
        <div class="wrapper-recipe">   
            <div class="container-recipe">
                <img src="{{item.image}}">
            </div>
        </div> 
        {% endfor %}

    <h2 class="subtitle">Mais acessadas</h2>

        

    </section>

{% endblock %}
